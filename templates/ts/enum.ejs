<%
const { valueList, name } = enumType;
const isLastIndex = (index) => index === valueList.length - 1;
const { enumFormat } = config;
_%>
<%_ if (enumFormat === "enum") { %>
export enum <%= name %> {
  <%_ valueList.forEach( (value, index) => { _%>
  <%=value.name%> = <%=value.number%><% if (!isLastIndex(index)) { %>, <% } %>
  <% }); _%>
}
<%_ } else { %>
export type <%= name %> = 
  <%_ valueList.forEach( (value, index) => { _%>
  <%=value.number%><% if (!isLastIndex(index)) { %> | <% } %>
  <% }); _%>
<% } %>